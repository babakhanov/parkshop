%ul.nav.navbar-nav.navbar-right
  - if user_signed_in?
    = render "shared/nav_link", to: destroy_user_session_path, text: "Logout"
  - else
    = render "shared/nav_link", to: new_user_session_path, text:"Sign In"
    = render "shared/nav_link", to: new_user_registration_path, text: "Sign Up"
